package com.wb.launcher3.weatherIcon;

public class ThunderIconDrawInfo extends WeatherIconDrawInfo {
    public ThunderIconDrawInfo(WeatherIcon icon) {
        super(icon);
    }

    public ThunderIconDrawInfo(WeatherIconDrawInfo iconDrawInfo) {
        super(iconDrawInfo);
    }
}
